
Процедура ОбработкаПроведения(Отказ, РежимПроведения)

	// регистр мрДанные
	Движения.мрДанные.Записывать = Истина;
	Для Каждого ТекСтрокаСостав Из Состав Цикл
		Движение = Движения.мрДанные.Добавить();
		Движение.Период = Дата;
		Движение.Город = Город;
		Движение.Товар = ТекСтрокаСостав.Товар;
		Движение.Количество = ТекСтрокаСостав.Количество;
		Движение.Цена = ТекСтрокаСостав.Цена;
		Движение.Сумма = ТекСтрокаСостав.Сумма;
	КонецЦикла;


КонецПроцедуры
